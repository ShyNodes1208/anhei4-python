# -*- coding: utf-8 -*-
"""
暗黑4 魔盒自动化 - 配置文件
分辨率基准：2560 x 1440（与原 G HUB 脚本一致）

所有坐标都是“屏幕实际像素”。如果游戏是 2560x1440 全屏/无边框，
屏幕坐标 == 游戏坐标，可直接沿用下面的值。
若点偏了，用 d4_capture.py 重新量一遍再改这里。
"""

import os

# ==================== 屏幕分辨率 ====================
SCREEN_W = 2560
SCREEN_H = 1440

# ==================== 背包处理范围（每次按实际背包改）====================
ROWS = 1
COLS = 2

# ==================== 阶段开关 ====================
DO_ADD_AFFIX = True   # 阶段1：添加词缀
DO_UPGRADE = True     # 阶段2：升级至传奇
DO_TRANSMUTE = True   # 阶段3：嬗变物品

# 添加词缀：连点“重塑”的总次数（加满4条后多余点击无效，不影响）
AFFIX_REFORGE_COUNT = 4

# ==================== 背包坐标 ====================
BAG_START_X = 1722
BAG_START_Y = 1017
BAG_STEP_X = 73
BAG_STEP_Y = 102

# ==================== 魔盒配方/按钮坐标 ====================
# 注意：放入装备后配方列表会重排，三个配方点法不同（沿用各自已验证的位置）
ADD_AFFIX_XY = (1138, 330)    # 加词缀：空盒时在配方列表顶部（先点配方，再放装备）
UPGRADE_XY = (1138, 1025)     # 升传奇：放入装备后的位置（先放装备，再点配方）
TRANSMUTE_XY = (1140, 510)    # 嬗变：放入装备后的位置（先放装备，再点配方）

REFORGE_XY = (468, 1140)      # “重塑”按钮
ACCEPT_XY = (1138, 822)       # “接受”按钮（弹窗中部左侧；实际会优先点识别到的位置）
CLEAR_XY = (480, 965)         # “清除”按钮：用它清空魔盒/取回装备，比右键取回安全
CUBE_ITEM_XY = (322, 480)     # 魔盒左上角装备格（已不再用于取回，保留备用）
SAFE_XY = (2103, 929)         # 鼠标安全停留位置

# ==================== 配方动态识别（按钮会随列表重排移动）====================
# 这三个配方在放入不同装备后位置会变，改用“找图再点”：在配方列区域里找到就点实时位置。
TPL_RECIPE_ADD_AFFIX = "recipe_add_affix.png"
TPL_RECIPE_UPGRADE = "recipe_upgrade.png"
TPL_RECIPE_TRANSMUTE = "recipe_transmute.png"

# 配方列表所在的搜索区域 (left, top, width, height)
REGION_RECIPE = (980, 280, 360, 1040)
# 配方模板匹配阈值（跨“可用/选中/变灰”状态，放宽一些）
RECIPE_THRESHOLD = 0.72

# ==================== 模板图（放在 templates/ 下）====================
TEMPLATE_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "templates")

# “接受”按钮模板
TPL_ACCEPT = "accept.png"
# 红字“该物品不适用于此配方组合”模板（裁红字的一小段即可）
TPL_NOT_APPLICABLE = "not_applicable.png"

# 模板匹配阈值（0~1，越高越严格）
MATCH_THRESHOLD = 0.80

# ==================== 识别搜索区域 (left, top, width, height) ====================
# 限定区域可以更快更准；坐标基于 2560x1440，按需微调。
# “接受”弹窗在屏幕中部：接受按钮≈(1138,822)，取消≈(1375,822)。
# 区域右边界控制在 1300 以内，避免把“取消”框进来。
REGION_ACCEPT = (1000, 760, 285, 140)       # “接受”按钮≈(1145,825)，右界1285避开“取消”
# 红字“该物品不适用于此配方组合”在“重塑”上方，≈(585,1065)。
REGION_NOT_APPLICABLE = (280, 1010, 640, 130)

# ==================== 时间配置（秒）====================
START_DELAY = 2.0           # 启动后等待
MOVE_DELAY = 0.20           # 鼠标移动到位后等待
STEP_DELAY = 1.0            # 每步点击后等待
BETWEEN_ITEMS_DELAY = 1.0   # 每件装备之间等待
BETWEEN_PHASES_DELAY = 1.0  # 每个阶段之间等待
ACCEPT_WAIT_TIMEOUT = 2.5   # 等待“接受”按钮出现的最长时间
DETECT_AFTER_PLACE = 0.6    # 放入装备后、判断是否“不适用”前的等待

# ==================== 热键 ====================
START_KEY = "f8"            # 按一下开始
STOP_KEY = "f10"            # 运行中按住/按下停止
